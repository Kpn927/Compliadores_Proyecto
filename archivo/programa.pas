program Hola;
var 
    x,y,z:integer;
    ELPEPEMAGICO:double;
begin
    readln(x);

     x := 0;
    if (x > 3) then
    begin
        if (y < 5) then
            z := 10;
    end;

for x := 0 to 150 do
    begin
        x := x + 1;
    end;
end.
