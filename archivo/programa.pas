program Hola;
var 
    x,y,z:integer;
    ELPEPEMAGICO:double;
begin
    x := 0;
    if (x > 3) then
    begin
        if (y < 5) then
            z := 10;
    end;


for x := 0 to 150 do
    begin
        x := x + 1;
        writeln(x);
    end;
// Hola, esto no deberia aparecer
/* Hola, esto tampoco deberia aparecer */
/* Esto si deberia aparecer
end.
