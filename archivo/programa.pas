program Hola;

procedure();
var
    x:integer;
begin
    writeln("");
end;

var 
    x,y,z,y1:integer;
    w:double;
begin

     x := 0;
    if (x > 3) then
    begin
        if (y < 5) then
            z := 10;
        else 
            z := 20;
    end;

for x := 0 to 150 do
    begin
        x := x + 1;
    end;

    writeln("");

    x := x + 1;
    y1 := 10;
    z := x + 10;
    y := ((((((x+y1))+(z*6/3)))) + 1) + 1 + ((2));

end.
