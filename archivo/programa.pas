program Hola;
var 
    x,y,z:integer;
    ELPEPEMAGICO:double;
begin

    writeln("Hola" +);

for i := 0 to 150 do
    begin
        x := x + 1;
    end;
// Hola, esto no deberia aparecer
(* Hola, esto tampoco deberia aparecer *)
{ Hola, esto tampoco deberia aparecer }
end.
