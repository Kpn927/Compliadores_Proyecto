program Hola;

function pruebas(x:integer; y1:double): integer;
var
  w1, result: integer;
  T: double;
begin
  writeln(w1);
  pruebas := w1; 
end;

var 
    x,y,z,y1:integer;
    w:double;
    test:character;
begin
    pruebas(1,2);
    test := 'h';
     x := 0;
     w := 3;
    if (x * 4) then
    begin
        if (y < 3) then
            z := 10;
        else 
            z := 20;
    end;

for x := 0 to 150 do
    begin
        x := x + 1;
    end;

    writeln('hola');

    x := x + 1;
    y1 := 10;
    z := x + 10;
    y := ((((((x+y1))+(z*6/3)))) + 1) + 1 + ((2));

end.
